
PatchManager
{
	// Required settings.  
	// srcPath should use forward slashes, and include the full file name.  srcPath should be in a directory 
	// called ".../PatchManager/PluginData"
	modName = LGG-InterstellarPack
	patchName = Blueshift Warp Range
	srcPath = ZZZ_InterstellarPack/PatchManager/PluginData/Blueshift_WarpRange.cfg
	shortDescr = Warp Range

	// Optional, but recommended
	longDescr = Warp to be sector-based instead of "jump anywhere".  Encourages Staging bases, Forward colonies, Interstellar infrastructure chains

	//// Optional entries here

	// dependencies, only show this patch if these specified mods are available
	// List either the directory of the mod (as show by ModuleManager), or the 
	// mod DLL (as show by ModuleManager)
	dependencies = Blueshift

	// Path to icon, if desired.  Can be a flag
	icon = ZZZ_InterstellarPack/Img/spacetux_flag

	// Author's name, if desired
	author = Linuxgurugamer 
}
